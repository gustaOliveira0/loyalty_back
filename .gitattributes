# Shell scripts must keep LF endings even when checked out on Windows,
# otherwise the Docker entrypoint shebang becomes "#!/bin/bash\r" and the
# container fails with: exec bin/docker-entrypoint.sh: no such file or directory
*.sh text eol=lf
bin/* text eol=lf
